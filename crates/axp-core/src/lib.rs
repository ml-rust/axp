//! `axp-core` — AXP runtime core.
//!
//! This crate provides the runtime machinery that drives agent execution.
//! It depends on `axp-proto` for protocol types.

mod error;

pub use error::{Error, Result};
