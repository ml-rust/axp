# /research is INTERNAL — strategy, raw findings, experiments, cloned repos. Never published.
research/

# local tooling config
.claude/settings.local.json

# Rust build artifacts (Cargo.lock is intentionally committed — this workspace ships a binary)
/target/

# scratch / temp
*.tmp
.DS_Store

# session prep scratch (never committed)
.claude/prep/
